#include "main.h"

/**
 * _strlen - returns len of given string
 * @s: pointer to char
 * Return: len of string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; i <= (*(s + 1)); i++)
	{
		return (i);
	}
	return (i);
}
