#include "main.h"

/**
 * sign - checks if number is +ve, -ve or 0
 * @n: number to check
 * Return: 1 exit code if character is lowercase; 0 otherwise
 */
int sign(int n)
{
	if (i > 0)
	{
		_putchar(43);
		_putchar(44);
		_putchar('\n');
		return (1);
	}
	else if (i == 0)
	{
		_putchar(48);
		_putchar(44);
		return (0);
	}
	else
	{
		_putchar(45);
		_putchar(44);
		return (-1);
	}
}
