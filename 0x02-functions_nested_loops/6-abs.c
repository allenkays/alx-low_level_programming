#include "main.h"

/**
 * _abs - outputs the absolute value of number given
 * @n: number to check
 * Return: 1 exit code if character is lowercase; 0 otherwise
 */
int _abs(int n)
{
	if (i >= 0)
	{
		return (n);
	}
	else
	{
		n *= -1;
		return (n);
	}
}
